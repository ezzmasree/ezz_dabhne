name: wibsite
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter_svg: ^1.1.0
  flutter:
    sdk: flutter
  http: ^1.2.2
  mongo_dart: ^0.10.3
  responsive_builder: ^0.7.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sign_in/
    - assets/
    - assets/sign_up/
